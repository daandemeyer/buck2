load(":rules.bzl", "simple_test")

simple_test(name = "test")

simple_test(
    name = "cacheable_test",
    seed = read_config("test", "seed", ""),
    supports_test_execution_caching = True,
)

simple_test(
    name = "failing_cacheable_test",
    exit_code = 1,
    supports_test_execution_caching = True,
)
