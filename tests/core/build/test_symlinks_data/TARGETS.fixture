load(":defs.bzl", "copy_source_dir", "copy_tree", "cp", "cp_via_builtin", "stat_path")

cp(name = "cp", src = "src/link/file")

cp_via_builtin(name = "cp_src_link_via_builtin", src = "src/link")

cp(name = "ext", src = "ext/link/file", local_only = True)

stat_path(
    name = "stat_symlink_in_dir",
    path = "src",
    project = "link",
)

stat_path(
    name = "stat_symlink",
    path = "src/link",
)

copy_tree(name = "ancestors", src = "ancestors")

copy_source_dir(
    name = "relocatable_copy",
    src = "relocatable",
    relative_symlinks = True,
)

copy_source_dir(
    name = "default_copy",
    src = "relocatable",
)

copy_tree(name = "cycle", src = "cycle")

stat_path(name = "loop", path = "loop/a/l1")

stat_path(name = "grow", path = "grow/a/l")

stub(
    name = "symlink_dep",
    deps = ["//testlink:dep"],
)
